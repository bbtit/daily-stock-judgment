# 16 — 今日の判断一覧と手動実行 UI

**What to build:** localhost で今日の判断一覧を見られ、手動で判断ランを起動できる。成功行はスコア・ラベル・理由、失敗行は区分のみ。

**Blocked by:** 15 — フェイクで日次判断ランナーが回る

**Status:** resolved

- [x] 今日の対象銘柄の判断結果が一覧表示される
- [x] 手動実行で判断ランを起動できる
- [x] 失敗行はデータ未着／取得不可／判断失敗の区分のみ（スコア等は出さない）
- [x] 同じ日の再実行後、成功結果が画面に反映される
